# Start Here

This folder is the real downstream site for `lotus-rise`.

The Once UI workspace is the upstream reference layer:

- `C:\Users\ryana\Desktop\AmalgamSites\once-ui`

## Read First

1. `PROJECT_SOURCE.md`
2. `AGENTS.md`
3. `docs/ONBOARDING.md`
4. `docs/AGENT_PROMPTS.md`
5. `docs/CLEANUP_CHECKLIST.md`

## What This Project Should Become

This repo should become one real product.
It should not stay as a starter demo with small edits on top.

## Theme And System Rule

Global design changes should start by checking `src/resources/once-ui.config.js` first, not scattered overrides.

## Workflow

1. understand the real page scope
2. remove or ignore out-of-scope starter surfaces
3. use the prompt pack with a filled-in project brief
4. implement
5. validate with build and lint
